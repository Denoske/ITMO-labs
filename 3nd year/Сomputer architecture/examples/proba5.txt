(defv a 2520)
(defv b 11)
(defv c 3)
(defv e 2520)
(defv d 11)
(loop secloop
    (loop firstloop
        (if (= b 0)
            (return-from firstloop)
        )
        (setq c b)
        (setq b (% a b))
        (setq a c)
    )
    (setq c (* (/ e a) d))
    (setq e c)
    (setq d (+ d 1))
    (setq a e)
    (setq b d)
    (if (= d 20)
        (return-from secloop)
    )
)
(print c)
